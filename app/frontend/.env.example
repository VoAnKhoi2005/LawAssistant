# Frontend local defaults
# Note: the current Flutter app reads API_BASE_URL from --dart-define.
# Keep this file only for local reference if you want environment notes in the frontend folder.

API_BASE_URL=http://localhost:8000
API_TIMEOUT=30000
